use crate::core::input::input::FileConts;
use std::io::Result;

pub fn insert_char(contents: &mut FileConts, input: char) -> Result<()> {
    Ok(())
}

pub fn insert_line(contents: &mut FileConts) -> Result<()> {
    Ok(())
}

pub fn backspace(contents: &mut FileConts) -> Result<()> {
    Ok(())
}

pub fn delete(contents: &mut FileConts) -> Result<()> {
    Ok(())
}
